language: php

sudo: required
dist: trusty

php:
  - 5.4

before_install:
  ##############################################################################
  # Setup apache2
  ##############################################################################
  - sudo apt-get update -qq
  - sudo apt-get install -qq -y --force-yes apache2 libapache2-mod-php5 php5-mysql php5-sqlite

  # Replace basedir in virtual host config
  - sed 's?%basedir%?'`pwd`'?' .travis/one.text.app.conf > .travis/one.text.app.conf
  - sed 's?%basedir%?'`pwd`'?' .travis/two.text.app.conf > .travis/two.text.app.conf

  # Define virtual host
  - sudo mv .travis/one.text.app.conf /etc/apache2/sites-available/one.text.app.conf
  - sudo mv .travis/two.text.app.conf /etc/apache2/sites-available/two.text.app.conf

  # Update our hosts
  - echo "127.0.0.1    one.test.app" | sudo tee -a /etc/hosts
  - echo "127.0.0.1    two.test.app" | sudo tee -a /etc/hosts

  # Enable mod rewrite
  - sudo a2enmod rewrite

  # Enable the sites
  - sudo a2ensite one.test.app.conf
  - sudo a2ensite two.test.app.conf

  # Restart apache
  - sudo /etc/init.d/apache2 restart

before_script:
  - curl http://one.test.app
  - curl http://two.test.app
